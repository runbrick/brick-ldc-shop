<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof title !== 'undefined' ? title : '砖头商城' %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: { DEFAULT: '#059669', 600: '#059669', 700: '#047857' },
            accent: '#F97316',
          }
        }
      }
    }
  </script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'DM Sans', system-ui, sans-serif; }
    .btn-primary { @apply bg-primary text-white px-4 py-2 rounded-lg font-medium hover:bg-primary-700 transition-colors duration-200 cursor-pointer; }
    .btn-accent { @apply bg-accent text-white px-4 py-2 rounded-lg font-medium hover:opacity-90 transition-opacity duration-200 cursor-pointer; }
    .card-hover { @apply transition-shadow duration-200 hover:shadow-lg cursor-pointer; }
  </style>
</head>
<body class="bg-slate-50 text-slate-900 min-h-screen">
  <nav class="bg-white border-b border-slate-200 sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-4 flex items-center justify-between h-14">
      <a href="/shop" class="font-semibold text-slate-900 text-lg cursor-pointer hover:text-primary transition-colors duration-200">砖头商城</a>
      <div class="flex items-center gap-4">
        <a href="/shop" class="text-slate-600 hover:text-primary transition-colors duration-200 cursor-pointer">首页</a>
        <% if (typeof user !== 'undefined' && user) { %>
          <a href="/shop/orders" class="text-slate-600 hover:text-primary transition-colors duration-200 cursor-pointer">我的订单</a>
          <a href="/admin" class="text-slate-600 hover:text-primary transition-colors duration-200 cursor-pointer">后台</a>
          <span class="text-slate-500 text-sm"><%= user.username %></span>
          <a href="/auth/linux-do/logout" class="text-slate-500 hover:text-slate-700 cursor-pointer">退出</a>
        <% } else { %>
          <a href="/shop/login" class="btn-primary text-sm">登录</a>
        <% } %>
      </div>
    </div>
  </nav>
  <main class="max-w-6xl mx-auto px-4 py-8">
    <%- body %>
  </main>
  <footer class="max-w-6xl mx-auto px-4 py-6 mt-auto border-t border-slate-200 text-slate-500 text-sm text-center">
    砖头商城 · Linux.do 登录 · credit.linux.do 支付
  </footer>
</body>
</html>
