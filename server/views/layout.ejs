<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof title !== 'undefined' ? title : '砖头商城' %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = { theme: { extend: { colors: { primary: { DEFAULT: '#0D9488', 700: '#0F766E' }, accent: '#C4A77D' } } } }
  </script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Lora:wght@400;500;600;700&family=Raleway:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Raleway', system-ui, sans-serif; }
    :root { --teal: #0D9488; --teal-dark: #0F766E; --paper: #F5F0E1; --kraft: #C4A77D; }
    .glass-page { min-height: 100vh; background: #F5F0E1; background-image: linear-gradient(165deg, rgba(13,148,136,0.06) 0%, transparent 35%, rgba(212,196,168,0.12) 100%); background-attachment: fixed; }
    .glass-card { background: rgba(255,252,248,0.92); border: 1px solid rgba(13,148,136,0.12); border-radius: 18px 14px 20px 16px; box-shadow: 0 4px 24px rgba(13,148,136,0.06); }
    .glass-nav { background: rgba(255,252,248,0.94); border: 1px solid rgba(13,148,136,0.1); border-radius: 20px 20px 14px 18px; margin: 1rem 1rem 0; box-shadow: 0 4px 20px rgba(13,148,136,0.05); }
    .btn-primary { background: var(--teal); color: #fff; padding: 0.75rem 1.5rem; border-radius: 12px 14px 10px 14px; font-weight: 500; cursor: pointer; }
    .btn-primary:hover { background: var(--teal-dark); }
    .btn-accent { background: var(--kraft); color: #fff; padding: 0.75rem 1.5rem; border-radius: 12px 14px 10px 14px; font-weight: 500; cursor: pointer; }
    .btn-accent:hover { background: #B5651D; }
    .card-hover { transition: box-shadow 0.25s ease; cursor: pointer; }
    .card-hover:hover { box-shadow: 0 12px 32px rgba(13,148,136,0.1); }
    .text-glass-primary { color: #1A1A1A; }
    .text-glass-muted { color: #4A4A4A; }
    .text-glass-soft { color: rgba(74,74,74,0.78); }
  </style>
</head>
<body class="text-glass-primary glass-page min-h-screen">
  <nav class="glass-nav sticky top-0 z-50">
    <div class="max-w-5xl mx-auto px-6 lg:px-10 flex items-center justify-between h-16">
      <a href="/" class="font-semibold text-glass-primary text-lg cursor-pointer hover:text-primary transition-colors duration-200" style="font-family: 'Lora', Georgia, serif;">砖头商城</a>
      <div class="flex items-center gap-6">
        <a href="/" class="text-glass-muted hover:text-primary cursor-pointer">首页</a>
        <% if (typeof user !== 'undefined' && user) { %>
          <a href="/orders" class="text-glass-muted hover:text-primary cursor-pointer">我的订单</a>
          <a href="/admin" class="text-glass-muted hover:text-primary cursor-pointer">后台</a>
          <span class="text-glass-soft text-sm"><%= user.username %></span>
          <a href="/auth/linux-do/logout" class="text-glass-soft hover:text-glass-primary cursor-pointer">退出</a>
        <% } else { %>
          <a href="/login" class="btn-primary text-sm">登录</a>
        <% } %>
      </div>
    </div>
  </nav>
  <main class="max-w-5xl mx-auto px-6 lg:px-10 py-12 lg:py-20 pt-28">
    <%- body %>
  </main>
  <footer class="max-w-5xl mx-auto px-6 lg:px-10 py-12 mt-auto border-t text-glass-soft text-sm text-center" style="border-color: rgba(13, 148, 136, 0.12);">
    砖头商城 · Linux.do 登录 · Linux.do 积分支付
  </footer>
</body>
</html>
