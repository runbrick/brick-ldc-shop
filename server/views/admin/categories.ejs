<%- include('partials/header', { title: '分类管理', user }) %>
<h1 class="text-xl font-bold text-platform-primary mb-8">分类管理</h1>
<p class="text-platform-soft mb-6 text-sm">一级分类不选父分类，二级分类选择父分类。商品归属到具体分类（一级或二级）。排序值越大越靠前。有子分类的一级不能直接删除。</p>
<% if (typeof query !== 'undefined' && query.err === 'has_children') { %>
<div class="platform-card p-4 mb-6 border border-amber-200 bg-amber-50/80 text-amber-900 text-sm">请先删除其下所有二级分类后再删除该一级分类。</div>
<% } %>

<form method="post" action="/admin/categories" class="platform-card p-6 mb-8 flex flex-wrap items-end gap-4 border border-slate-200">
  <div>
    <label class="block text-platform-muted font-medium mb-1 text-sm">父分类</label>
    <select name="parent_id" class="platform-input px-3 py-2 w-40">
      <option value="">无（一级分类）</option>
      <% (categoryTree || []).forEach(function(p) { %>
      <option value="<%= p.id %>"><%= p.name %></option>
      <% }); %>
    </select>
  </div>
  <div>
    <label class="block text-platform-muted font-medium mb-1 text-sm">名称</label>
    <input type="text" name="name" required placeholder="如：虚拟卡密" maxlength="32" class="platform-input px-3 py-2 w-48" />
  </div>
  <div>
    <label class="block text-platform-muted font-medium mb-1 text-sm">排序</label>
    <input type="number" name="sort" value="0" class="platform-input px-3 py-2 w-24" />
  </div>
  <button type="submit" class="btn-primary cursor-pointer">添加</button>
</form>

<div class="platform-card overflow-hidden border border-slate-200">
  <table class="w-full text-left">
    <thead class="border-b border-slate-200 bg-slate-50">
      <tr>
        <th class="px-4 py-3 text-platform-muted font-medium text-sm">ID</th>
        <th class="px-4 py-3 text-platform-muted font-medium text-sm">层级</th>
        <th class="px-4 py-3 text-platform-muted font-medium text-sm">名称 / 排序</th>
        <th class="px-4 py-3 text-platform-muted font-medium text-sm">操作</th>
      </tr>
    </thead>
    <tbody>
      <% (categoryTree || []).forEach(function(p) { %>
      <tr class="border-b border-slate-200 bg-slate-50/50">
        <td class="px-4 py-3 text-platform-primary"><%= p.id %></td>
        <td class="px-4 py-3 text-platform-soft text-sm">一级</td>
        <td class="px-4 py-3">
          <form method="post" action="/admin/categories/<%= p.id %>" class="inline-flex flex-wrap items-center gap-2">
            <input type="hidden" name="parent_id" value="" />
            <input type="text" name="name" value="<%= p.name %>" required maxlength="32" class="platform-input px-3 py-1.5 w-40 text-sm" />
            <input type="number" name="sort" value="<%= p.sort %>" class="platform-input px-3 py-1.5 w-20 text-sm" title="排序" />
            <button type="submit" class="text-primary hover:underline cursor-pointer text-sm">保存</button>
          </form>
        </td>
        <td class="px-4 py-3">
          <form method="post" action="/admin/categories/<%= p.id %>/delete" class="inline" onsubmit="return confirm('确定删除？其下二级分类将保留为一级。');">
            <button type="submit" class="text-red-600 hover:underline cursor-pointer text-sm">删除</button>
          </form>
        </td>
      </tr>
      <% (p.children || []).forEach(function(c) { %>
      <tr class="border-b border-slate-200 hover:bg-slate-50">
        <td class="px-4 py-3 pl-10 text-platform-primary"><%= c.id %></td>
        <td class="px-4 py-3 text-platform-soft text-sm">二级</td>
        <td class="px-4 py-3">
          <form method="post" action="/admin/categories/<%= c.id %>" class="inline-flex flex-wrap items-center gap-2">
            <input type="hidden" name="parent_id" value="<%= p.id %>" />
            <input type="text" name="name" value="<%= c.name %>" required maxlength="32" class="platform-input px-3 py-1.5 w-40 text-sm" />
            <input type="number" name="sort" value="<%= c.sort %>" class="platform-input px-3 py-1.5 w-20 text-sm" title="排序" />
            <button type="submit" class="text-primary hover:underline cursor-pointer text-sm">保存</button>
          </form>
        </td>
        <td class="px-4 py-3">
          <form method="post" action="/admin/categories/<%= c.id %>/delete" class="inline" onsubmit="return confirm('确定删除？使用该分类的商品将变为未分类。');">
            <button type="submit" class="text-red-600 hover:underline cursor-pointer text-sm">删除</button>
          </form>
        </td>
      </tr>
      <% }); %>
      <% }); %>
    </tbody>
  </table>
  <% if (!categoryTree || categoryTree.length === 0) { %>
  <div class="p-16 text-center text-platform-soft">暂无分类，请在上方添加</div>
  <% } %>
</div>
<%- include('partials/footer') %>
